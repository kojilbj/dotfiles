-- Using Hack Nerd Font (already installed) instead of the reference's SF
-- Pro/SF Symbols setup, to avoid an extra font/icon-system dependency.
return {
	paddings = 6,
	font = {
		text = "Hack Nerd Font",
		numbers = "Hack Nerd Font",
		style_map = {
			["Regular"] = "Regular",
			["Semibold"] = "Bold",
			["Bold"] = "Bold",
			["Black"] = "Bold",
		},
	},
}
